package com.reverse.javacode;
// Package → organizes class in project structure

public class ReverseUsingLoop {

    public static void main(String[] args) {

        String input = "Hello Shabina";
        // Input string → we want to reverse this

        String rev = "";
        // Empty string to store reversed result
        // Initially blank, we will keep adding characters


        for (int i = input.length() - 1; i >= 0; i--) {
            // Loop starts from last index

            // input.length() → total characters
            // length() - 1 → last index (because index starts from 0)

            // i-- → move backward (right → left)
            // i >= 0 → loop until first character

        }}}
